<p style="color: green"><%= notice %></p>

<h1>Books</h1>

<%= button_to "List All Books", books_path %> 

<div id="books">
  <% @books.each do |book| %>
    <%= render book %>
    <p>
      <%= link_to "Show this book", book %>
    </p>
  <% end %>
</div>

<%= link_to "New book", new_book_path %>
